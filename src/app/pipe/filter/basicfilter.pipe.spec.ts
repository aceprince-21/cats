import { BasicfilterPipe } from './basicfilter.pipe';

describe('BasicfilterPipe', () => {
  it('create an instance', () => {
    const pipe = new BasicfilterPipe();
    expect(pipe).toBeTruthy();
  });
});
